{
  "name": "webscout-discovery-workflow",
  "version": "2.0.0",
  "description": "Website Discovery Workflow — Find businesses across the US that need new or updated websites",
  "scripts": {
    "install:all": "npm install --prefix client",
    "dev": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client"
  },
  "keywords": ["web-design", "lead-generation", "discovery", "freelance", "business", "outreach"],
  "license": "MIT"
}
